import{NextResponse}from'next/server';import{createAdminSession,verifyAdminPassword}from'@/lib/admin/auth';export async function POST(req:Request){try{const{password}=await req.json();if(typeof password!=='string'||!verifyAdminPassword(password))return NextResponse.json({ok:false,error:'รหัสผ่านไม่ถูกต้อง'},{status:401});return NextResponse.json({ok:true,token:await createAdminSession()});}catch(e){return NextResponse.json({ok:false,error:e instanceof Error?e.message:'LOGIN_FAILED'},{status:400});}}
