import{NextResponse}from'next/server';import{hashAdminSession,requireAdmin}from'@/lib/admin/auth';import{getServerSupabase}from'@/lib/supabase/server';export async function POST(req:Request){try{await requireAdmin(req);const token=req.headers.get('x-neramit-admin')!;const db=getServerSupabase();await db.from('admin_sessions').delete().eq('token_hash',hashAdminSession(token));await db.from('admin_audit_logs').insert({action:'admin_logout'});return NextResponse.json({ok:true});}catch{return NextResponse.json({ok:false,error:'ADMIN_REQUIRED'},{status:401});}}
