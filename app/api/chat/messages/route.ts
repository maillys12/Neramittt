import{NextResponse}from'next/server';import{readDeviceToken}from'@/lib/http/device';import{resolveDevice}from'@/lib/device/server';import{getServerSupabase}from'@/lib/supabase/server';export async function GET(req:Request){try{const device=await resolveDevice(readDeviceToken(req));const draftId=new URL(req.url).searchParams.get('draft');if(!draftId)throw new Error('DRAFT_REQUIRED');const db=getServerSupabase();const{data:draft,error}=await db.from('drafts').select('id').eq('id',draftId).eq('device_id',device.id).single();if(error)throw error;const{data:messages,error:msgError}=await db.from('chat_messages').select('role,content,created_at').eq('draft_id',draft.id).order('created_at',{ascending:true});if(msgError)throw msgError;return NextResponse.json({ok:true,messages});}catch(e){return NextResponse.json({ok:false,error:e instanceof Error?e.message:'CHAT_FAILED'},{status:404});}}
