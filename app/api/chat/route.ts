import{NextResponse}from'next/server';import{z}from'zod';import{readDeviceToken}from'@/lib/http/device';import{resolveDevice}from'@/lib/device/server';import{getServerSupabase}from'@/lib/supabase/server';import{getOpenAI}from'@/lib/openai/client';const S=z.object({draftId:z.string().uuid(),message:z.string().min(1).max(5000)});export async function POST(req:Request){try{const device=await resolveDevice(readDeviceToken(req));const input=S.parse(await req.json());const db=getServerSupabase();const{data:draft,error}=await db.from('drafts').select('id,brief').eq('id',input.draftId).eq('device_id',device.id).single();if(error)throw error;await db.from('chat_messages').insert({draft_id:draft.id,role:'user',content:input.message});const{data:setting}=await db.from('settings').select('value').eq('key','chat_max_messages').single();const limit=Math.max(2,Math.min(100,Number(setting?.value??30)));const{data:messages}=await db.from('chat_messages').select('role,content').eq('draft_id',draft.id).order('created_at',{ascending:false}).limit(limit);const chronological=[...(messages??[])].reverse();const ai=await getOpenAI().responses.create({model:'gpt-5-mini',instructions:'คุณคือ Neramit ผู้ช่วยเก็บ creative brief สำหรับสร้างภาพและโปสเตอร์ ตอบภาษาไทย กระชับ ถามเฉพาะข้อมูลสำคัญที่ยังขาด ห้ามถามซ้ำสิ่งที่ผู้ใช้ตอบแล้ว ห้ามแต่งข้อมูลแทนผู้ใช้ เมื่อข้อมูลพอแล้วให้สรุป brief และบอกว่าสามารถไปขั้นถัดไปได้',input:chronological.map(m=>`${m.role}: ${m.content}`).join('\n')});const text=ai.output_text||'รับข้อมูลแล้วครับ';await db.from('chat_messages').insert({draft_id:draft.id,role:'assistant',content:text});await db.from('drafts').update({brief:{...(draft.brief as Record<string,unknown>),conversation_summary:text},updated_at:new Date().toISOString()}).eq('id',draft.id).eq('device_id',device.id);return NextResponse.json({ok:true,message:text});}catch(e){return NextResponse.json({ok:false,error:e instanceof Error?e.message:'CHAT_FAILED'},{status:400});}}
