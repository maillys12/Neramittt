import{NextResponse}from'next/server';import{getServerSupabase}from'@/lib/supabase/server';export async function GET(req:Request){if(req.headers.get('authorization')!==`Bearer ${process.env.CRON_SECRET}`)return NextResponse.json({ok:false},{status:401});const db=getServerSupabase();const now=new Date().toISOString();const{data:refs}=await db.from('reference_images').select('id,storage_path').lt('expires_at',now).limit(200);if(refs?.length){await db.storage.from('reference-images').remove(refs.map(x=>x.storage_path));await db.from('reference_images').delete().in('id',refs.map(x=>x.id));}await db.from('drafts').delete().lt('expires_at',now);await db.from('admin_sessions').delete().lt('expires_at',now);return NextResponse.json({ok:true,removedReferences:refs?.length??0});}
