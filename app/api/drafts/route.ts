import {NextResponse}from'next/server';import{draftInputSchema}from'@/lib/drafts/schema';import{readDeviceToken}from'@/lib/http/device';import{resolveDevice}from'@/lib/device/server';import{getServerSupabase}from'@/lib/supabase/server';export async function POST(req:Request){try{const device=await resolveDevice(readDeviceToken(req));const input=draftInputSchema.parse(await req.json());const{data,error}=await getServerSupabase().from('drafts').insert({device_id:device.id,mode:input.mode,title:input.title??null,brief:input.brief,state:'editing',expires_at:new Date(Date.now()+30*864e5).toISOString()}).select('*').single();if(error)throw error;return NextResponse.json({ok:true,draft:data});}catch(e){return NextResponse.json({ok:false,error:e instanceof Error?e.message:'UNKNOWN_ERROR'},{status:400});}}
