import{NextResponse}from'next/server';import{z}from'zod';import{readDeviceToken}from'@/lib/http/device';import{resolveDevice}from'@/lib/device/server';import{getServerSupabase}from'@/lib/supabase/server';import{getOpenAI}from'@/lib/openai/client';import{selectedReferenceAnalysis}from'@/lib/references/selected';import{parseGenerationResult}from'@/lib/generate/result';import{languageInstruction,normalizeCreationSettings,platformInstruction}from'@/lib/ui/creation-settings';import{generationPromptPolicy,sanitizeModelText}from'@/lib/ui/image-prompt-policy';
const S=z.object({draftId:z.string().uuid(),requestId:z.string().min(8).max(120),variantCount:z.number().int().min(1).max(3).default(1)});
export async function POST(req:Request){let requestId='',deviceId='';const db=getServerSupabase();try{const device=await resolveDevice(readDeviceToken(req));deviceId=device.id;const input=S.parse(await req.json());requestId=input.requestId;const{data:draft,error}=await db.from('drafts').select('*').eq('id',input.draftId).eq('device_id',device.id).single();if(error)throw error;const settings=normalizeCreationSettings(draft.brief&&typeof draft.brief==='object'?draft.brief:{});const variantCount=input.variantCount||settings.variantCount;const[refsQuery,settingQuery]=await Promise.all([db.from('reference_images').select('analysis,usage_options').eq('draft_id',draft.id).eq('device_id',device.id),db.from('settings').select('value').eq('key','daily_quota').single()]);if(refsQuery.error)throw refsQuery.error;const selected=selectedReferenceAnalysis(refsQuery.data??[]);const{data:reserved,error:reserveError}=await db.rpc('reserve_prompt_quota',{p_device_id:device.id,p_draft_id:draft.id,p_request_id:input.requestId,p_variant_count:variantCount,p_daily_limit:Number(settingQuery.data?.value??10)});if(reserveError)throw reserveError;const row=reserved?.[0];if(row?.is_existing){const{data:job}=await db.from('prompt_jobs').select('*').eq('request_id',input.requestId).eq('device_id',device.id).single();return NextResponse.json({ok:true,job});}if(!row?.job_id)throw new Error('QUOTA_RESERVATION_FAILED');await db.from('prompt_jobs').update({status:'processing'}).eq('id',row.job_id).eq('device_id',device.id);const prompt=`คุณคือ Neramit ผู้เชี่ยวชาญการเขียนพรอมต์สำหรับ AI สร้างภาพ\n${platformInstruction(settings.platform)}\n${languageInstruction(settings.language)}\n${generationPromptPolicy()}\nสร้าง ${variantCount} แบบ โดยแต่ละแบบต้องแตกต่างกันอย่างมีประโยชน์ ห้ามแต่งข้อเท็จจริงที่ผู้ใช้ไม่ได้ให้มา หาก brief มีข้อความภาษาไทย ให้ตีความเป็นข้อมูลสำหรับจัดพื้นที่ว่างสำหรับใส่ข้อความภายหลังเท่านั้น ห้ามสั่งโมเดลภาพเขียนข้อความไทยลงภาพ ตอบ JSON เท่านั้นในรูป {"variants":[{"label":"แบบที่ 1","prompt":"Subject & Medium: ...\\nElements & Details: ...\\nColors & Lighting: ...\\nComposition & Layout: ...\\nText Placeholder Instructions: ...\\nParameters: --ar 4:5"}]} โดยค่า prompt ของทุกแบบต้องเป็นภาษาอังกฤษล้วน\nBRIEF:${JSON.stringify(draft.brief)}\nSELECTED REFERENCE ANALYSIS:${JSON.stringify(selected)}`;const ai=await getOpenAI().responses.create({model:'gpt-5.6-luna',reasoning:{effort:'low'},store:false,max_output_tokens:1800,input:prompt});const result=parseGenerationResult(sanitizeModelText(ai.output_text),variantCount);const completedAt=new Date().toISOString();const[jobWrite,promptWrite]=await Promise.all([db.from('prompt_jobs').update({status:'completed',result,completed_at:completedAt}).eq('id',row.job_id).eq('device_id',device.id),db.from('prompts').insert({device_id:device.id,draft_id:draft.id,prompt_job_id:row.job_id,title:draft.title,variants:result.variants,input_summary:{...draft.brief,target_platform:settings.platform,prompt_language:'en',variant_count:variantCount}})]);if(jobWrite.error)throw jobWrite.error;if(promptWrite.error)throw promptWrite.error;await db.rpc('finalize_prompt_quota',{p_request_id:input.requestId,p_success:true});return NextResponse.json({ok:true,result,settings:{...settings,language:'en',variantCount}});}catch(e){if(requestId&&deviceId){await Promise.all([db.from('prompt_jobs').update({status:'failed',error_message:e instanceof Error?e.message:'GENERATE_FAILED',completed_at:new Date().toISOString()}).eq('request_id',requestId).eq('device_id',deviceId),db.rpc('finalize_prompt_quota',{p_request_id:requestId,p_success:false})]);}return NextResponse.json({ok:false,error:e instanceof Error?e.message:'GENERATE_FAILED'},{status:400});}}
