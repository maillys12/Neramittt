import{NextResponse}from'next/server';import{randomUUID}from'node:crypto';import{readDeviceToken}from'@/lib/http/device';import{resolveDevice}from'@/lib/device/server';import{getServerSupabase}from'@/lib/supabase/server';const allowed=new Set(['image/jpeg','image/png','image/webp']);export async function POST(req:Request){try{const device=await resolveDevice(readDeviceToken(req));const form=await req.formData();const draftId=String(form.get('draftId')||'');const file=form.get('file');if(!(file instanceof File)||!allowed.has(file.type))throw new Error('รองรับเฉพาะ JPEG, PNG และ WebP');const db=getServerSupabase();const{data:draft}=await db.from('drafts').select('id').eq('id',draftId).eq('device_id',device.id).single();if(!draft)throw new Error('ไม่พบดราฟต์');const{count}=await db.from('reference_images').select('id',{count:'exact',head:true}).eq('draft_id',draftId);if((count??0)>=4)throw new Error('อัปโหลดได้สูงสุด 4 ภาพ');const{data:max}=await db.from('settings').select('value').eq('key','max_reference_mb').single();if(file.size>Number(max?.value??8)*1024*1024)throw new Error('ไฟล์มีขนาดใหญ่เกินกำหนด');const ext=file.type==='image/png'?'png':file.type==='image/webp'?'webp':'jpg';const path=`${device.id}/${draftId}/${randomUUID()}.${ext}`;const{error:uploadError}=await db.storage.from('reference-images').upload(path,file,{contentType:file.type,upsert:false});if(uploadError)throw uploadError;const{data,error}=await db.from('reference_images').insert({draft_id:draftId,device_id:device.id,storage_path:path,mime_type:file.type,size_bytes:file.size,expires_at:new Date(Date.now()+30*864e5).toISOString()}).select('id,storage_path,mime_type,size_bytes').single();if(error){await db.storage.from('reference-images').remove([path]);throw error;}return NextResponse.json({ok:true,reference:data});}catch(e){return NextResponse.json({ok:false,error:e instanceof Error?e.message:'UPLOAD_FAILED'},{status:400});}}
