import Link from'next/link';export default async function References({searchParams}:{searchParams:Promise<{draft?:string}>}){const{draft}=await searchParams;return <main className="shell"><Link href="/">← หน้าแรก</Link><section className="card pageCard"><span className="eyebrow">🖼 ภาพอ้างอิง</span><h1>เพิ่มภาพอ้างอิง</h1><p>ขั้นตอนนี้ไม่บังคับ คุณสามารถใช้ภาพได้สูงสุด 4 ภาพ</p><div style={{display:'flex',gap:12,flexWrap:'wrap'}}><Link className="btn" href={`/review?draft=${draft??''}`}>ข้ามขั้นตอนนี้</Link><Link className="btn primary" href={`/review?draft=${draft??''}`}>ไปตรวจสอบ →</Link></div></section></main>}
