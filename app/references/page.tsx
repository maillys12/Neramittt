import ReferencesClient from'@/components/ReferencesClient';import{AppHeader}from'@/components/ui/AppHeader';import{NeramitIcon}from'@/components/ui/NeramitIcon';
export default async function References({searchParams}:{searchParams:Promise<{draft?:string}>}){const{draft}=await searchParams;return <main className="siteShell innerPage"><AppHeader/><section className="pageIntro"><span className="pageMascot pageMascot--vector"><NeramitIcon name="search" size={30}/></span><div><h1>ตรวจ<span className="gradientText">ภาพอ้างอิง</span></h1><p>เลือกสิ่งที่อยากนำไปใช้ และแก้ข้อมูลที่ AI อ่านได้</p></div></section>{draft?<ReferencesClient draftId={draft}/>:<div className="card">ไม่พบดราฟต์</div>}</main>}
