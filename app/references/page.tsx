import Link from'next/link';import ReferencesClient from'@/components/ReferencesClient';export default async function References({searchParams}:{searchParams:Promise<{draft?:string}>}){const{draft}=await searchParams;return <main className="shell"><Link href="/">← หน้าแรก</Link><section className="card pageCard"><span className="eyebrow">🖼 ภาพอ้างอิง</span><h1>เพิ่มภาพอ้างอิง</h1><p>ไม่บังคับ รองรับ JPEG, PNG และ WebP สูงสุด 4 ภาพ</p>{draft?<ReferencesClient draftId={draft}/>:<p>ไม่พบดราฟต์</p>}</section></main>}
