import Link from'next/link';import ResultClient from'@/components/ResultClient';export default async function Result({searchParams}:{searchParams:Promise<{draft?:string}>}){const{draft}=await searchParams;return <main className="shell"><section className="card pageCard"><span className="eyebrow">✦ สำเร็จ</span><h1>พรอมต์ของคุณพร้อมแล้ว</h1>{draft&&<ResultClient draftId={draft}/>}<p><Link className="btn" href="/history">ดูประวัติ</Link> <Link className="btn primary" href="/">สร้างใหม่</Link></p></section></main>}
