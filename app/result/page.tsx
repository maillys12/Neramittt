import ResultClient from'@/components/ResultClient';import{AppHeader}from'@/components/ui/AppHeader';
export default async function Result({searchParams}:{searchParams:Promise<{draft?:string}>}){const{draft}=await searchParams;return <main className="siteShell innerPage"><AppHeader/><section className="pageIntro"><span className="pageMascot">📋</span><div><h1>พรอมต์ของคุณ<span className="gradientText">พร้อมแล้ว!</span></h1><p>เลือกแบบที่ชอบ แล้วคัดลอกไปใช้กับ AI</p></div></section>{draft?<ResultClient draftId={draft}/>:<div className="card">ไม่พบผลลัพธ์</div>}</main>}
