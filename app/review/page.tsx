import ReviewClient from'@/components/ReviewClient';import{AppHeader}from'@/components/ui/AppHeader';
export default async function Review({searchParams}:{searchParams:Promise<{draft?:string}>}){const{draft}=await searchParams;return <main className="siteShell innerPage"><AppHeader quota="10 ครั้งวันนี้"/><section className="pageIntro"><span className="pageMascot">📋</span><div><h1>ตรวจ<span className="gradientText">สรุปก่อนสร้าง</span></h1><p>เช็กไอเดียให้ตรงใจ แล้วให้เนรมิตเขียนพรอมต์</p></div></section>{draft?<ReviewClient draftId={draft}/>:<div className="card">ไม่พบดราฟต์</div>}</main>}
