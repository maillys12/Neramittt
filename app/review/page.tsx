import ReviewClient from'@/components/ReviewClient';export default async function Review({searchParams}:{searchParams:Promise<{draft?:string}>}){const{draft}=await searchParams;return <main className="shell"><section className="card pageCard"><span className="eyebrow">✓ ตรวจสอบก่อนสร้าง</span><h1>รายละเอียดพรอมต์</h1>{draft?<ReviewClient draftId={draft}/>:<p>ไม่พบดราฟต์</p>}</section></main>}
