import ReviewClient from'@/components/ReviewClient';import{AppHeader}from'@/components/ui/AppHeader';import{NeramitIcon}from'@/components/ui/NeramitIcon';
export default async function Review({searchParams}:{searchParams:Promise<{draft?:string}>}){const{draft}=await searchParams;return <main className="siteShell innerPage"><AppHeader/><section className="pageIntro"><span className="pageMascot pageMascot--vector"><NeramitIcon name="check" size={30}/></span><div><h1>ตรวจ<span className="gradientText">สรุปก่อนสร้าง</span></h1><p>เช็กไอเดียให้ตรงใจ แล้วให้เนรมิตเขียนพรอมต์</p></div></section>{draft?<ReviewClient draftId={draft}/>:<div className="card">ไม่พบดราฟต์</div>}</main>}
