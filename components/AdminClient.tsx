'use client';import{useState}from'react';export default function AdminClient(){const[token,setToken]=useState('');const[password,setPassword]=useState('');const[metrics,setMetrics]=useState<any>();async function login(){const r=await fetch('/api/admin/login',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({password})});const x=await r.json();if(!x.token)return alert(x.error||'เข้าสู่ระบบไม่สำเร็จ');setToken(x.token);const d=await fetch('/api/admin/dashboard',{headers:{'x-neramit-admin':x.token}}).then(r=>r.json());setMetrics(d.metrics);}if(!token)return <div className="formStack"><label>รหัสผ่านผู้ดูแล<input type="password" value={password} onChange={e=>setPassword(e.target.value)}/></label><button className="btn primary" onClick={login}>เข้าสู่ระบบ</button></div>;return <div className="cards"><div className="card"><strong>อุปกรณ์</strong><h2>{metrics?.devices??'…'}</h2></div><div className="card"><strong>พรอมต์</strong><h2>{metrics?.prompts??'…'}</h2></div><div className="card"><strong>รูปอ้างอิง</strong><h2>{metrics?.references??'…'}</h2></div><div className="card"><strong>ข้อผิดพลาด</strong><h2>{metrics?.errors??'…'}</h2></div></div>}
