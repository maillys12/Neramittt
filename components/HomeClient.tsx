'use client';import {useEffect,useState} from 'react';import Link from 'next/link';import {getOrCreateDeviceToken} from '@/lib/device/token';
export default function HomeClient(){const[quota,setQuota]=useState('…');useEffect(()=>{const token=getOrCreateDeviceToken();fetch('/api/device',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({token})}).then(r=>r.json()).then(x=>setQuota(`${x.used??0}/${x.limit??10}`)).catch(()=>setQuota('—'));},[]);return <main className="shell"><header className="top"><div className="brand">✦ Neramit</div><div className="quota">โควตาวันนี้ {quota}</div></header><section className="hero"><div><span className="eyebrow">✦ เปลี่ยนไอเดียให้เป็นพรอมต์</span><h1>สร้างพรอมต์ดี ๆ<br/><span className="gradient">ได้ง่ายกว่าที่คิด</span></h1><p>บอก Neramit ว่าคุณอยากได้ภาพหรือโปสเตอร์แบบไหน แล้วให้เราช่วยจัดรายละเอียดให้พร้อมนำไปใช้กับ AI ที่คุณเลือก</p></div><div className="mascot">✏️</div></section><section className="cards"><article className="card"><h2>💬 คุยกับ Neramit</h2><p>เล่าไอเดียแบบธรรมชาติ แล้ว AI จะถามเฉพาะข้อมูลที่ยังขาด</p><Link className="btn primary" href="/chat">เริ่มคุย</Link></article><article className="card"><h2>✦ แบบฟอร์มด่วน</h2><p>กรอกหัวข้อ สไตล์ สี สัดส่วน และข้อความที่ต้องการได้ทันที</p><Link className="btn" href="/form">เปิดแบบฟอร์ม</Link></article></section></main>}
