'use client';import{useState}from'react';import{useRouter}from'next/navigation';import{getOrCreateDeviceToken}from'@/lib/device/token';export default function QuickForm(){const router=useRouter();const[busy,setBusy]=useState(false);async function submit(fd:FormData){setBusy(true);const brief={work_type:fd.get('work_type'),topic:fd.get('topic'),poster_text:fd.get('poster_text'),preserve_text:fd.get('preserve_text')==='on',additional_details:fd.get('additional_details'),style:fd.get('style'),color:fd.get('color'),composition:fd.get('composition'),ratio:fd.get('ratio')};const r=await fetch('/api/drafts',{method:'POST',headers:{'content-type':'application/json','x-neramit-device':getOrCreateDeviceToken()},body:JSON.stringify({mode:'form',title:String(brief.topic||''),brief})});const x=await r.json();setBusy(false);if(x.draft?.id)router.push(`/references?draft=${x.draft.id}`);else alert('บันทึกดราฟต์ไม่สำเร็จ');}return <form action={submit} className="formStack"><label>ประเภทงาน<input name="work_type" placeholder="เช่น โปสเตอร์ประชาสัมพันธ์"/></label><label>หัวข้อ<input name="topic" required placeholder="หัวข้องานของคุณ"/></label><label>ข้อความบนโปสเตอร์<textarea name="poster_text" rows={5}/></label><label className="check"><input type="checkbox" name="preserve_text" defaultChecked/> คงข้อความตามที่กรอก</label><label>รายละเอียดเพิ่มเติม<textarea name="additional_details" rows={4}/></label><label>สไตล์<select name="style"><option>สดใส</option><option>มินิมอล</option><option>โมเดิร์น</option><option>น่ารัก</option><option>หรูหรา</option></select></label><label>โทนสี<input name="color" placeholder="เช่น ส้ม ม่วง"/></label><label>การจัดวาง<input name="composition"/></label><label>สัดส่วน<select name="ratio"><option>1:1</option><option>4:5</option><option>9:16</option><option>A4 แนวตั้ง</option></select></label><button className="btn primary" disabled={busy}>{busy?'กำลังบันทึก…':'ไปภาพอ้างอิง →'}</button></form>}
