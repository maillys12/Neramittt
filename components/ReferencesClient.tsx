'use client';import{useState}from'react';import Link from'next/link';import{getOrCreateDeviceToken}from'@/lib/device/token';type Ref={id:string;size_bytes:number;analysis?:Record<string,unknown>;usage_options?:Record<string,boolean>};const aspects=['colors','style','composition','subjects','background','visible_text'];export default function ReferencesClient({draftId}:{draftId:string}){const[items,setItems]=useState<Ref[]>([]);const[busy,setBusy]=useState(false);async function setAspect(ref:Ref,key:string,value:boolean){const usage={...(ref.usage_options||{}),[key]:value};setItems(v=>v.map(x=>x.id===ref.id?{...x,usage_options:usage}:x));await fetch(`/api/references/${ref.id}`,{method:'PATCH',headers:{'content-type':'application/json','x-neramit-device':getOrCreateDeviceToken()},body:JSON.stringify({usageOptions:usage})});}async function analyze(id:string){setBusy(true);const r=await fetch(`/api/references/${id}/analyze`,{method:'POST',headers:{'x-neramit-device':getOrCreateDeviceToken()}});const x=await r.json();setBusy(false);if(x.analysis)setItems(v=>v.map(y=>y.id===id?{...y,analysis:x.analysis,usage_options:Object.fromEntries(aspects.map(k=>[k,true]))}:y));else alert(x.error||'วิเคราะห์ไม่สำเร็จ');}async function upload(file:File){setBusy(true);const fd=new FormData();fd.set('draftId',draftId);fd.set('file',file);const r=await fetch('/api/references',{method:'POST',headers:{'x-neramit-device':getOrCreateDeviceToken()},body:fd});const x=await r.json();setBusy(false);if(x.reference)setItems(v=>[...v,x.reference]);else alert(x.error||'อัปโหลดไม่สำเร็จ');}async function remove(id:string){await fetch(`/api/references/${id}`,{method:'DELETE',headers:{'x-neramit-device':getOrCreateDeviceToken()}});setItems(v=>v.filter(x=>x.id!==id));}return <><label className="upload">{busy?'กำลังทำงาน…':'＋ เลือกรูปอ้างอิง'}<input hidden type="file" accept="image/jpeg,image/png,image/webp" disabled={busy||items.length>=4} onChange={e=>e.target.files?.[0]&&upload(e.target.files[0])}/></label><div className="refList">{items.map(x=><div className="ref" key={x.id}><div><span>🖼 {Math.round(x.size_bytes/1024)} KB</span>{x.analysis&&<div className="aspects">{aspects.map(k=><label key={k}><input type="checkbox" checked={x.usage_options?.[k]??true} onChange={e=>setAspect(x,k,e.target.checked)}/>{k}</label>)}</div>}</div><div><button onClick={()=>analyze(x.id)} disabled={busy}>วิเคราะห์</button><button onClick={()=>remove(x.id)}>ลบ</button></div></div>)}</div><Link className="btn primary" href={`/review?draft=${draftId}`}>ไปตรวจสอบ →</Link></>}
