'use client';import{useState}from'react';import Link from'next/link';import{getOrCreateDeviceToken}from'@/lib/device/token';export default function ReferencesClient({draftId}:{draftId:string}){const[items,setItems]=useState<any[]>([]);const[busy,setBusy]=useState(false);async function upload(file:File){setBusy(true);const fd=new FormData();fd.set('draftId',draftId);fd.set('file',file);const r=await fetch('/api/references',{method:'POST',headers:{'x-neramit-device':getOrCreateDeviceToken()},body:fd});const x=await r.json();setBusy(false);if(x.reference)setItems(v=>[...v,x.reference]);else alert(x.error||'อัปโหลดไม่สำเร็จ');}async function remove(id:string){await fetch(`/api/references/${id}`,{method:'DELETE',headers:{'x-neramit-device':getOrCreateDeviceToken()}});setItems(v=>v.filter(x=>x.id!==id));}return <><label className="upload">{busy?'กำลังอัปโหลด…':'＋ เลือกรูปอ้างอิง'}<input hidden type="file" accept="image/jpeg,image/png,image/webp" disabled={busy||items.length>=4} onChange={e=>e.target.files?.[0]&&upload(e.target.files[0])}/></label><div className="refList">{items.map(x=><div className="ref" key={x.id}><span>🖼 {Math.round(x.size_bytes/1024)} KB</span><button onClick={()=>remove(x.id)}>ลบ</button></div>)}</div><Link className="btn primary" href={`/review?draft=${draftId}`}>ไปตรวจสอบ →</Link></>}
