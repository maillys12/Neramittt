'use client';import{useEffect,useState}from'react';import{getBrowserSupabase}from'@/lib/supabase/browser';export default function NotificationsClient(){const[n,setN]=useState<any[]>([]);async function t(){return(await getBrowserSupabase().auth.getSession()).data.session?.access_token||''}async function load(){const token=await t();if(!token)return location.href='/login';const x=await fetch('/api/member/notifications',{headers:{authorization:`Bearer ${token}`}}).then(r=>r.json());setN(x.notifications||[])}useEffect(()=>{load()},[]);async function read(id?:string){const token=await t();await fetch('/api/member/notifications',{method:'PATCH',headers:{authorization:`Bearer ${token}`,'content-type':'application/json'},body:JSON.stringify({id})});load()}return <div className="memberPanel"><div className="memberPanelHead"><h1>การแจ้งเตือน</h1><button onClick={()=>read()}>อ่านทั้งหมด</button></div>{n.length?n.map(x=><button className={`notificationRow ${x.read_at?'':'unread'}`} key={x.id} onClick={()=>read(x.id)}><b>{x.title}</b><span>{x.message}</span><small>{new Date(x.created_at).toLocaleString('th-TH')}</small></button>):<p className="mutedText">ยังไม่มีการแจ้งเตือน</p>}</div>}
