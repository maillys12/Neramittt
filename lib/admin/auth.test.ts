import{describe,expect,it}from'vitest';import{hashAdminSession}from'./auth';describe('admin auth',()=>{it('hashes sessions deterministically without storing raw token',()=>{process.env.DEVICE_TOKEN_PEPPER='test-pepper';expect(hashAdminSession('secret-session')).toBe(hashAdminSession('secret-session'));expect(hashAdminSession('secret-session')).not.toContain('secret-session');});});
