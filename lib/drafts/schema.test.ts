import{describe,expect,it}from'vitest';import{draftInputSchema}from'./schema';describe('draft input',()=>{it('accepts a form creative brief',()=>{const parsed=draftInputSchema.parse({mode:'form',brief:{topic:'โปสเตอร์'}});expect(parsed.mode).toBe('form');expect(parsed.brief.topic).toBe('โปสเตอร์');});it('rejects an unsupported mode',()=>expect(()=>draftInputSchema.parse({mode:'other',brief:{}})).toThrow());});
