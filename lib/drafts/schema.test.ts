import {describe,expect,it} from 'vitest';import {draftInputSchema} from './schema';describe('draft input',()=>{it('accepts form brief and 1-3 variants',()=>{expect(draftInputSchema.parse({mode:'form',variantCount:2,brief:{topic:'โปสเตอร์'}}).variantCount).toBe(2)});it('rejects more than 3 variants',()=>expect(()=>draftInputSchema.parse({mode:'form',variantCount:4,brief:{}})).toThrow());});
