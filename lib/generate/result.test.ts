import{describe,expect,it}from'vitest';import{parseGenerationResult}from'./result';describe('generation result',()=>{it('accepts exact requested variant count',()=>expect(parseGenerationResult('{"variants":[{"label":"A","prompt":"one"},{"label":"B","prompt":"two"}]}',2).variants).toHaveLength(2));it('rejects mismatched multi-variant output',()=>expect(()=>parseGenerationResult('{"variants":[{"label":"A","prompt":"one"}]}',2)).toThrow());});
