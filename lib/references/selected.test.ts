import{describe,expect,it}from'vitest';import{selectedReferenceAnalysis}from'./selected';describe('selected reference analysis',()=>{it('keeps only user-selected aspects plus confidence notes',()=>{expect(selectedReferenceAnalysis([{analysis:{colors:['red'],style:'minimal',confidence_notes:'high'},usage_options:{colors:true,style:false}}])).toEqual([{colors:['red'],confidence_notes:'high'}]);});});
